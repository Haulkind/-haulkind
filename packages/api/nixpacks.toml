[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['cd ../.. && pnpm install --frozen-lockfile']

[phases.build]
cmds = ['cd ../.. && pnpm build']

[start]
cmd = 'cd ../.. && node dist/server/_core/index.js'
